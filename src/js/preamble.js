window.app = window.app || {};

(function (app) {
    "use strict";

    // Containers for backbone objects.
    app.model = {};
    app.view = {};
    app.collection = {};
    app.router = {};
}(window.app));
