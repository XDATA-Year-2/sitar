/* global Backbone */

(function (app) {
    "use strict";

    app.views = {};

    app.views.Gallery = Backbone.View.extend({
    });

    app.views.GalleryItem = Backbone.View.extend({
    });
}(window.app));
